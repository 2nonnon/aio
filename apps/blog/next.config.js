// @ts-check

/**
 * @type {import('next').NextConfig}
 */
const nextConfig = {
  // experimental: {
  //   typedRoutes: true,
  // },
  // transpilePackages: ['@non/ui', '@non/hooks', '@non/utils'],
}

module.exports = nextConfig
